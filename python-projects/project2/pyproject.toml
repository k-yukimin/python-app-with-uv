[project]
name = "project2"
version = "0.1.0"
description = "Second Python project scaffold managed by uv"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.2",
]

[dependency-groups]
dev = [
    "mypy>=1.11",
    "pytest>=8.3",
    "ruff>=0.6",
]

[project.scripts]
project2 = "project2.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/project2"]

[tool.mypy]
cache_dir = "/tmp/mypy-cache"
mypy_path = ["src"]
strict = true

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
